groups:
- name: test-rule
  rules:
  - alert: Mysql Uptime
    expr: mysql_global_status_uptime < 3600
    labels:
      team: node
    annotations:
      summary: "{{$labels.instance}}: mysql went down"
      description: "{{$labels.instance}}: mysql went down {{ $value }} ago"
